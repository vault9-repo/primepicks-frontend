import React from "react";

function Dashboard() {
  return (
    <div
      style={{
        height: "100vh",
        backgroundImage: "url('/assets/BACKGROUND.PNG')",
        backgroundSize: "cover",
        backgroundPosition: "center",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        color: "#fff",
      }}
    >
      <h1>Welcome to PrimePicks Dashboard</h1>
    </div>
  );
}

export default Dashboard;
